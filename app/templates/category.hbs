<div id="category-wrapper" class="container text-center">

  <div class="row text-center">
    <h1>{{model.name}}</h1>
  </div>

  {{#each model.questions as |question|}}
    {{question.title}}
  {{/each}}
</div>
